/*
  Lesson 2 — Exercise 1: Basic JSX
  Goals:
  - Render a simple header and a paragraph using JSX
  - Keep it minimal; focus on correct JSX syntax
  - Optionally add a small inline style or an attribute
  - Try it in a playground like the NextLeap React.js compiler: https://nextleap.app/online-compiler/reactjs-programming

  Import/export setup is intentionally not provided here — figuring that out is part of the challenge.
*/

/* eslint-disable no-unused-vars */
function ExerciseOneBasic() {
  return (
    <section>
      {/* TODO: Add a header (e.g., <h1> or <h2>) with a short title */}
      {/* TODO: Add a paragraph (<p>) with one or two sentences */}

      {/* Example ideas (do not copy verbatim):
          <h1>My First JSX</h1>
          <p>This is a paragraph rendered with JSX.</p>
        
        Bonus:
          - Add a <strong>highlighted</strong> word
          - Add a simple inline style to the header or paragraph
      */}
    </section>
  );
}

export default ExerciseOneBasic;

